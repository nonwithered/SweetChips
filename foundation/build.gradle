apply plugin: 'java-library'
apply from: rootProject.file('scripts/publish.groovy')

dependencies {
    implementation project(':utility')
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}
