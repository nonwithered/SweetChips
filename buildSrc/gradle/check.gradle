import java.nio.file.Files
import java.nio.file.Paths

ext {

    applyPlugin4Gradle = Files.exists(Paths.get(
            "${rootProject.projectDir}",
            '..',
            'build',
            'org',
            'sweetchips',
            'plugin4gradle',
            '1.0.0',
            'plugin4gradle-1.0.0.jar'
    ))
    applyVisitors = Files.exists(Paths.get(
            "${rootProject.projectDir}",
            '..',
            'build',
            'org',
            'sweetchips',
            'visitors',
            '1.0.0',
            'visitors-1.0.0.jar'
    ))
}