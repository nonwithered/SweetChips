import java.nio.file.Files
import java.nio.file.Paths

ext {

    applyPlugin4Gradle = Files.exists(Paths.get(
            "$projectDir",
            '..',
            'build',
            'org',
            'sweetchips',
            'plugin4gradle',
            '1.0.0',
            'plugin4gradle-1.0.0.jar'
    ))
}