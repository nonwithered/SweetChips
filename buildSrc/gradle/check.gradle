import java.nio.file.Files

ext {
    applyPlugin4Gradle = Files.exists(rootProject.projectDir.toPath()
            .resolve('..')
            .resolve('build')
            .resolve('org')
            .resolve('sweetchips')
            .resolve('plugin4gradle')
            .resolve('1.0.0')
            .resolve('plugin4gradle-1.0.0.jar')
    )
    applyVisitors = Files.exists(rootProject.projectDir.toPath()
            .resolve('..')
            .resolve('build')
            .resolve('org')
            .resolve('sweetchips')
            .resolve('visitors')
            .resolve('1.0.0')
            .resolve('visitors-1.0.0.jar')
    )
}