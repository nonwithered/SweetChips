buildscript {
    ext.kotlin_version = '1.3.61'
    repositories {
        maven {
            url gradle.buildUrl
        }
        google()
        jcenter()
//        mavenCentral()
    }
    dependencies {
    }
}

allprojects {
    repositories {
        maven {
            url gradle.buildUrl
        }
        google()
        jcenter()
//        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
